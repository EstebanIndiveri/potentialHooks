import React from 'react';

const TodoReducer = (state=[],action) => {

    switch (action.type) {
        case 'add':
            break;
    
        default:
            return state;
    }
}
 
export default TodoReducer;